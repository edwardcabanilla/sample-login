It's a best practice to store API keys in the local.properties file instead of hardcoding them into your source code, as this keeps sensitive information out of version control and improves security.

I added hashing of password when saving to local database
